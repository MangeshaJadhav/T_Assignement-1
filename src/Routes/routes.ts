// import { Express, Request, Response, NextFunction } from "express";
// import getUserHandler from "../Controller/user.controller";

// function routes(app: Express) {
//   app.get("/api/getuser", getUserHandler);
// }

// export default routes;
